/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: '/path1',
  trailingSlash: true,
  // compiler: {
  //   removeConsole: false,
  // },
}

module.exports = nextConfig
